<UserControl x:Class="TurtleGraphics.XAML.InteliCommandsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TurtleGraphics.Models"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
			 Visibility="{Binding Visible}">

	<UserControl.DataContext>
		<local:InteliCommandDialogViewModel/>
	</UserControl.DataContext>
    <Grid>
		<ListBox ItemsSource="{Binding Source}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<StackPanel Background="Beige" Orientation="Horizontal">
						<TextBlock Text="{Binding Selected}" Margin="0 0 5 0"/>
						<TextBlock Text="{Binding HintText}"/>
					</StackPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
    </Grid>
</UserControl>
